# AttributeCSS

AttributeCSS uses css attr function to build the next generation atom css library.

🚧 The css function `attr()` that can be used with any CSS property is only supported with chrome 133 and higher. So don't use it in your production project. [more here](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/attr#examples)
